// Example/test-only acceptance seam. It is intentionally absent from the
// package root exports and its native command is inert in Release builds.
export { NativeCommitAcceptanceContainer } from './primitives';
