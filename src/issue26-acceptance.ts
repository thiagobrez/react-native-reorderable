/**
 * Private debug-only entrypoint used by the example acceptance surface.
 * It is intentionally absent from the package exports and public index.
 */
export { FallbackCommitAcceptanceContainer } from './primitives';
