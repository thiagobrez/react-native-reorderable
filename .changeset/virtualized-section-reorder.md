---
'react-native-reorderable': minor
---

Add the SectionList-shaped `ReorderableSectionList` with section-aware rendering, section chrome geometry, empty-section destinations, and atomic cross-section reorder.
