---
"react-native-reorderable": minor
---

Add the FlatList-shaped `ReorderableList` with exact identity geometry, retained virtualization, and stationary edge auto-scroll across off-window destinations.
